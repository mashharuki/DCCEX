import { CreateTokenContent } from "@/components/createToken/content";
import type { NextPage } from "next";

/**
 * CreateToken Component
 * @returns 
 */
const CreateToken : NextPage = () => {
  return <CreateTokenContent />;
};

export default CreateToken;
